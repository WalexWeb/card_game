<div>
	<h1>🎴 WordMatch: Онлайн карточная игра</h1>
	<p>Современная мультиплеерная игра для тренировки ассоциативного мышления и командного взаимодействия</p>
</div>

---

## 📝 Назначение

WordMatch — это онлайн карточная игра, в которой игроки соревнуются в угадывании слов и значений, используя ассоциации и командную работу. Игра отлично подходит для вечеринок, тимбилдинга и развития креативного мышления.

## 🛠️ Используемый стек технологий

**Frontend:**
- React
- TypeScript
- Vite
- Tailwind CSS 

**Backend:**
- Node.js
- Express
- Socket.IO

**DevOps и инфраструктура:**
- Docker
- Nginx (reverse proxy для клиента)

## 📦 Структура проекта

- `client/` — клиентская часть (интерфейс, логика игры, взаимодействие с сервером)
- `server/` — серверная часть (логика игры, обработка событий, WebSocket)
- `docker-compose.yml` — запуск всего приложения в контейнерах

## 🚀 Быстрый старт

### 1. Клонирование репозитория
```bash
git clone <repo-url>
cd card-game
```

### 2. Запуск через Docker (рекомендуется)
```bash
docker-compose up --build
```
- Клиент: http://localhost:3600
- Сервер: http://localhost:8600

### 3. Локальный запуск без Docker

#### Сервер
```bash
cd server
npm install
npm start
```

#### Клиент
```bash
cd client
bun install
bun run dev
```

## 🗂️ Ключевые файлы и папки
- `client/src/app/components/` — основные React-компоненты игрового интерфейса
- `client/src/app/hooks/useSocket.ts` — хук для работы с WebSocket
- `server/src/socket/gameHandler.js` — обработка игровых событий через Socket.IO
- `server/src/utils/deckGenerator.js` — генерация колоды карт
